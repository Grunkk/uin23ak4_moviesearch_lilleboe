export default function Layout({children}) {
    return(
        <>
        <header>
        <h1>Filmsøk!</h1>
        </header>
        <main>
            {children}
        </main>

        </>
    )
}